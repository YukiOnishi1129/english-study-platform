substitutions:
  _IMAGE: "web-admin:latest"
  _SERVICE_NAME: "web-admin-application"

steps:
  - name: "gcr.io/cloud-builders/docker"
    entrypoint: "bash"
    args:
      - "-c"
      - |
        docker build -t $_REGION-docker.pkg.dev/$PROJECT_ID/$_IMAGE -f web/admin/Dockerfile .

  - name: "gcr.io/cloud-builders/docker"
    args: ["push", "$_REGION-docker.pkg.dev/$PROJECT_ID/$_IMAGE"]

  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: gcloud
    args:
      [
        "run",
        "deploy",
        "$_SERVICE_NAME",
        "--image",
        "$_REGION-docker.pkg.dev/$PROJECT_ID/$_IMAGE",
        "--region",
        "$_REGION",
        "--quiet",
      ]

images:
  - $_REGION-docker.pkg.dev/$PROJECT_ID/$_IMAGE

options:
  logging: CLOUD_LOGGING_ONLY
